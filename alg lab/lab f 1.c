#include<stdio.h>
int main(){

   int i, j,n,V;
   printf("Enter coin size: ");
   scanf("%d",&n);
   int a[n+1];
   printf("Coin array: \n");
   for(i=1; i<n+1; i++) {
            printf("Enter value for a[%d]]:", i);
            scanf("%d", &a[i]);
    T[0] [0]=0;
    for (i=1;i<=n;i++)
    T [i] [0] = 0;
    for (j=1;j<=v;j++)
    T [0] [j] = 99999;
   }
   printf("Enter amount: ");
   scanf("%d",&V);
   int  b[n+1][V+1];

   for(i=0; i<n+1; i++) {
      for(j=0;j<V+1;j++)
        {
            b[i][j]=0;
            printf(" %d ", b[i][j]);

      }
      printf("\n");
   }
   return 0;
}
